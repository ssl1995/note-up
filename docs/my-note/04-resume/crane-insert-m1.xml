<p><b>方案对比（为什么不选别的）</b>：</p>
<ul>
<li><b>为什么不用 if-else / switch 分发</b>：违反开闭原则，每加一类审批流都要改主流程代码，回归风险大、代码越来越臃肿。</li>
<li><b>为什么不用手动注册</b>：易遗漏、注册顺序不可控；@PostConstruct 自注册让"新增子类即接入"，主流程零改动（2025 年新增 Tableau 拆分审批流不足 200 行实证）。</li>
<li><b>工厂为什么用 ConcurrentHashMap</b>：注册集中在启动期（写），运行期纯读，并发读无锁、性能好。</li>
</ul>
